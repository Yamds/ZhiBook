// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AccountKind } from "./AccountKind";

/**
 * 账户：资金载体，归属某个账本。
 *
 * `balance_cents` 不落库：读取时按「初始余额 + 关联账单累计」聚合得到，
 * 避免出现与账单不一致的冗余余额。
 */
export type Account = { id: string, bookId: string, kind: AccountKind, name: string, iconName: string, 
/**
 * `theme` 或 `#RRGGBB`。
 */
color: string, initialBalanceCents: number, 
/**
 * 当前余额（分）；资产 / 负债均以正数表示量级，可为负。
 */
balanceCents: number, sortOrder: number, createdAtMs: number, updatedAtMs: number, };
