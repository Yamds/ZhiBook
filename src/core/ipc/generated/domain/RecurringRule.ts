// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { EntryKind } from "./EntryKind";

/**
 * 固定收支规则：每天 05:00 自动记一笔。
 *
 * `start_day` = 创建时刻之后的**下一个 05:00** 所在日（绝不早于创建时刻）；
 * `last_run_day` = 最近一次已生成（或已跳过）的日期。
 * 补账从 `max(start_day, last_run_day + 1)` 开始，`recurring_runs` 保幂等。
 */
export type RecurringRule = { id: string, bookId: string, kind: EntryKind, amountCents: number, note: string, categoryId: string, 
/**
 * 可选关联账户（null = 未指定）。
 */
accountId: string | null, enabled: boolean, startDay: string, 
/**
 * 最近一次已处理的日期；null = 从未处理。
 */
lastRunDay: string | null, createdAtMs: number, updatedAtMs: number, };
