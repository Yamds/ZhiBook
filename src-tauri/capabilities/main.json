{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "main",
  "description": "主窗口能力",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "opener:default",
    "opener:allow-open-url"
  ]
}
